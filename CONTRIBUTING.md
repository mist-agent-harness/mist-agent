# 怎么参与 mist

mist 现在在**建造阶段**（2026-08-14 开门）：第一里程碑是最小垂直闭环，
写代码的规矩见「代码阶段」一节。提愿望、调研、设计仍然走许愿池流程，两轨并行。

## 许愿池阶段的流程

1. **提 issue**：愿望、调研、设计提案都走 issue。调研请注明是实查（跑了代码/
   翻了仓库）还是读文档，两者证据等级不同。
2. **收编**：主笔定期把达成共识的内容收进 `docs/`，然后在 issue 里留一句
   「收编到哪里」，关闭 issue。**真相只在 docs，issue 是流水**——想了解项目
   现状，读 docs，不用翻 issue 历史。
3. **待拍板**：没拍板的问题集中挂在 [docs/decisions.md](docs/decisions.md)，
   那是一唯一的看板。拍板权在主笔。
4. **PR**：文档增量直接提 PR，评审过就并，不挂过一轮收编周期。

## 写文档的规矩

- 「实证」二字只给验收清单跑出来的东西用。读过源码 ≠ 验过。
- 每条设计决定写代价。不知道代价的决定还没做完。
- 术语以 [docs/glossary.md](docs/glossary.md) 为准，新术语先进术语表再进正文。
- 文档是给人读的：少分层，说人话，一份文档一个主题。

## 代码阶段

2026-08-14 开门。规矩从少立起，用坏了再补：

1. **验收先行**。里程碑的判卷程序（`acceptance/`）先于功能代码存在，
   「做完了」以 `npm run acceptance` 的绿灯为准，不以描述为准。
   验收清单怎么读、怎么改，见 [acceptance/README.md](acceptance/README.md)。
2. **认领整件**。闭环拆出的零件在 issue 里认领，认的是「让它能跑、让它的灯变绿」，
   不是「给它写文档」。认了两天不动工，主笔可以收回重发。
3. **小 PR**。一个 PR 干一件事，附一句「这个 PR 让哪盏灯（或哪条测试）变了颜色」。
   提交前本地过 `npm run lint && npm run typecheck && npm test`，CI 同款三件套。
4. **语言栈** 按决策 D3：TypeScript 进主仓，Python 生态走 sidecar 不进主仓。
5. **不做最小实现**（主笔 2026-08-14 立）。认领一件就交付完整：正常路径、
   边界输入、错误处理、测试，一次补齐。「先跑通再说」的半成品不许进主干——
   你的「再说」就是下一个人的地雷。
6. **不做最小修复**（主笔 2026-08-14 立）。修就修干净。什么叫最小修复：
   只堵报错的那一格，病根留着，下次换个姿势发作。修复的完整动作是：
   找到病根、同类的洞一起排掉、用测试把洞钉死。这个项目人多、AI 多、
   速度快，唯一扛得住的方式就是每一手都干净。
